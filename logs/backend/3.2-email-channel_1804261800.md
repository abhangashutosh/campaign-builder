# Task 3.2 — Email Channel Service

**Status:** DONE
**Agent:** Backend Dev
**Date:** 2026-04-18

## What was built
- `apps/api/src/channels/email/email.types.ts`
  - `EmailSendOptions`, `EmailSendResult`, `EmailSendError`, `IEmailProvider`
  - `EmailSendError.retryable = statusCode >= 500` (4xx non-retryable)
- `apps/api/src/channels/email/email.service.ts`
  - Wraps Resend SDK
  - Throws `EmailSendError` with correct `retryable` flag
  - Retry logic lives in BullMQ processor (not in service)

## Security
- HTML is never constructed from raw user input
- React Email props are HTML-escaped by React

## Acceptance criteria
- [x] Resend 4xx → throws EmailSendError with retryable: false
- [x] Network error → treated as 503 retryable
- [x] Retry logic in processor, not service
