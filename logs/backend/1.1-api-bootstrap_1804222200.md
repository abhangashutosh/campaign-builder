# Task 1.1 — NestJS Bootstrap + Global Config

**Status:** DONE
**Agent:** Backend Dev
**Date:** 2026-04-18

## What was built
- main.ts with helmet, compression, ValidationPipe, HttpExceptionFilter, ResponseEnvelopeInterceptor
- app.module.ts with TypeORM, BullMQ, ConfigModule (Joi validation)
- common/config: app.config.ts + database.config.ts
- common/guards: JwtAuthGuard + RolesGuard
- common/decorators: @Public(), @Roles(), @TenantId()
- common/filters: HttpExceptionFilter (structured error response)
- common/interceptors: ResponseEnvelopeInterceptor (wraps all responses)
- common/repositories: TenantScopedRepository base class
- database/data-source.ts (TypeORM CLI)
- All 8 migrations (contacts → segments → templates → channel_configs → campaigns → journeys → campaign_deliveries → delivery_events)
- All entities: Contact, Segment, Template, ChannelConfig, Campaign, CampaignDelivery, DeliveryEvent, Journey
- All modules: Contacts, Segments, Templates, Campaigns, Journeys, Settings, Health
- DomainVerificationService with SPF/DKIM/DMARC checks
- EventsGateway (Socket.IO)
- Unit tests: JwtAuthGuard, TenantScopedRepository, DomainVerificationService

## Acceptance criteria
- [x] HttpExceptionFilter returns { success:false, error: { code, message, statusCode }, meta.requestId }
- [x] ResponseEnvelopeInterceptor wraps all responses in { success:true, data, meta }
- [x] All 8 migrations in correct dependency order
- [x] TenantScopedRepository throws when tenantId is empty
- [x] DomainVerificationService scores: all pass=100, SPF+DKIM=67, DMARC partial=83
