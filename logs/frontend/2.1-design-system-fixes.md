# 2.1 Design System Fixes

**Date:** 2026-04-19  
**Status:** DONE  
**Area:** frontend/ui

## Changes

### CSS Design Tokens (globals.css)
- Fixed `--border-2` from `#F1F5F9` → `#EDF1F6` (exact design HTML value)
- Fixed `--hover` from `#F8FAFC` → `#F1F5F9` (exact design HTML value)
- Fixed `--muted` from `#CBD5E1` → `#94A3B8` (exact design HTML value)
- Fixed `--shadow-md` and `--shadow-sm` to match design HTML exact values
- Added `--shadow-lg`, `--sidebar-w`, `--header-h` tokens
- Added body font-smoothing and letter-spacing matching design

### Sidebar (sidebar.tsx)
- Rewrote to use design HTML CSS classes (`.sidebar`, `.brand`, `.tenant`, `.nav`, etc.)
- Fixed active nav state: now uses `navy-50` bg + `navy` text (was incorrectly using full navy bg)
- Added tenant/workspace selector card
- Added usage/sends bar in footer
- Added correct badge/count styles for nav items

### Header (header.tsx)
- Rewrote to use design HTML CSS classes (`.header`, `.crumbs`, `.search`, `.icon-btn`, `.user-menu`)
- Added breadcrumb navigation
- Correct search bar dimensions and styling

### AppShell (app-shell.tsx)
- Updated to use CSS grid layout matching design HTML `.app` structure
