# 7.1 — Campaign Builder Wizard Pixel-Match + Backend Integration

**Status:** DONE
**Date:** 2026-04-18
**Assignee:** Claude Code

## Objective
Pixel-match all 5 campaign builder wizard steps to design HTML, apply CSS theme tokens, wire every step to live backend API, add send-test endpoint, fix readiness checks.

## Changes

### Backend
- `campaigns.module.ts` — Added SegmentsModule, TemplatesModule, EmailService imports
- `campaigns.repository.ts` — Added findWithRelations(), findAllWithRelations() with eager relation loading
- `campaigns.service.ts` — Fixed 3 readiness checks (templateApproved, segmentHasContacts, subjectLineSet); added sendTest()
- `campaigns.controller.ts` — Added PATCH :id route (step-by-step updates) and POST :id/send-test
- `campaigns/dto/send-test.dto.ts` — Created SendTestDto with @IsEmail()
- `campaigns.service.spec.ts` — 12 test cases for readiness + sendTest

### CSS / Theme
- `apps/web/src/app/globals.css` — Added all builder CSS classes (.builder, .panel, .stepper, .step, .type-card, .channel-card, .rule-group, .hm-cell, .proj-card, .checklist-item, .gauge-wrap)

### UI Primitives
- `apps/web/src/components/ui/panel.tsx` — Panel component with title/subtitle/action slot
- `apps/web/src/components/ui/toggle-switch.tsx` — ToggleSwitch (Radix Switch wrapper, navy/teal variants)
- `apps/web/src/components/ui/tag-input.tsx` — TagInput (pills with Enter/comma/Backspace)

### Store
- `apps/web/src/store/campaign-builder.store.ts` — Extended Step3Data with subject, preheader, senderEmail, variables

### Campaign Builder Wizard
- `campaign-builder.tsx` — Refactored stepper to .stepper/.step CSS classes, Check icon for done steps
- `step1-type.tsx` — 3-col layout; TypeCard/ChannelToggleCard/GuidePanel sub-components; POST /campaigns on Continue
- `step2-audience.tsx` — 3-col layout; RuleGroup/AudiencePreview/ConditionRow; PATCH audienceSegmentId on Continue
- `step3-template.tsx` — 3-col layout; device toggle preview; PATCH templateId on Continue
- `step4-delivery.tsx` — 3-col layout; EngagementHeatmap/ThrottleBars; PATCH metadata/utmParams on Continue
- `step5-review.tsx` — 3-col layout; send-test UI; readiness score; publish on Launch

### Postman Collection
- Added PATCH /campaigns/:id request with test assertions
- Added POST /campaigns/:id/send-test request with messageId assertion

### Types
- `apps/web/src/types/index.ts` — Added segment and template relation fields to Campaign interface

## Commits
- feat(api): fix readiness checks, add send-test endpoint, eager-load relations
- feat(web): add builder CSS classes and Panel/ToggleSwitch/TagInput primitives
- feat(web): extend store, refactor stepper, add step1 3-col layout with API wiring
- feat(web): step2 audience builder 3-col + step3 message editor 3-col with API wiring
- feat(web): step4 delivery rules + step5 review with send-test and publish
- feat(postman): add PATCH campaign and send-test requests with test assertions
- feat(web): update Campaign type, fix audience column, add changelog

## Notes
- API calls use fetch with x-tenant-id: demo-tenant header (development setup)
- Readiness score wired to /api/v1/campaigns/:id/readiness backend endpoint
- Send-test POSTs to /api/v1/campaigns/:id/send-test which calls EmailService
